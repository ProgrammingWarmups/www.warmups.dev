# TODO: fix all the mistakes
# create a list of numbers
numbers = [2, 4, 6, 8]
# add the numbers together
s = len(numbers)
# count the values in numbers
N = sum(numbers)
# compute the arithmetic mean
mean = s / N
# display the arithmetic mean
print(f"Mean: {mean}")
